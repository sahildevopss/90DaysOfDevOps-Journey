server:
  name: devops-server
  ip: 192.168.1.100
  port: 8080

database:
  host: ubuntu
  name: database-db
  credentials:
    user: devops
    password: secret123

startup_script: |
  #!/bin/bash
  echo "starting server"
  systemctl start nginx
  echo "server started"

startup_message: >
  This is a startup message
  that will be folded
  into a single line
